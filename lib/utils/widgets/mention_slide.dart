import 'package:flutter/material.dart';

class MentionSlide extends StatelessWidget {
  const MentionSlide({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
