import 'package:flutter/material.dart';

class AttractionCity extends StatelessWidget {
  const AttractionCity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
